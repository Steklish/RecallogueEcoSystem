# ================================
# === llama.cpp servers (C++ llama-server)
# ================================
MODEL_ROLE=model
# Чатовый сервер (llama-server)
LLAMACPP_CHAT_BASE=http://192.168.0.3:11434
# Если запускаешь через -m / путь к GGUF — укажи basename файла,
# если через -hf — можно указать тег репозитория/модели.
LLAMACPP_CHAT_MODEL=MODEL_NAME_HERE.gguf

# Сервер эмбеддингов (llama-server с флагом --embedding)
LLAMACPP_EMBED_BASE=http://192.168.0.3:11435
# Имя модели для OpenAI-стиля /v1/embeddings (обычно basename GGUF)
LLAMACPP_EMBED_MODEL=MODEL_NAME_HERE.gguf

# for gemini cli
GEMINI_API_KEY="xxx" 
# for my dumb model replacement
GEMINI_MODEL="gemini-2.0-flash-lite"
# Ипользуемая модель Gemini для тестов в замену слабых локальных моделей

USE_GEMINI=0

QWEN_MODEL_OPENROUTER="qwen/qwen-2.5-coder-32b-instruct:free"

USE_QWEN=1

QWEN_TOKEN="xxxx"

# ================================
# === Storage (локальные пути)
# ================================
CHROMA_PERSIST_DIR=./storage/chroma
STORAGE_RAW_DIR=./storage/raw
STORAGE_TEXT_DIR=./storage/text

# ================================
# === Чанкинг и поиск
# ================================
# Размер чанка (в словах) и перекрытие (в словах)
CHUNK_SIZE=800
CHUNK_OVERLAP=120
# Сколько результатов брать из векторального поиска
TOP_K=4
# Лимит символов контекста, который подставляем в промпт
MAX_CONTEXT_CHARS=12000

# ================================
# === Клиенты llama-server: таймауты / ретраи / большие файлы
# ================================
# Параметры эмбеддинга (используются в app/embed_server.py)
# Сколько текстов отправлять за один HTTP-запрос к /v1/embeddings
EMBED_BATCH=2
# Жёсткий лимит длины одного элемента для эмбеддинга (символы)
EMBED_MAX_CHARS=4096

# Общие сетевые настройки клиентов (chat/embeddings)
# Таймаут запроса к llama-server (сек)
LLAMACPP_TIMEOUT_S=300
# Сколько раз повторять попытку при ошибках сети/HTTP
LLAMACPP_MAX_RETRIES=3


# ================================
# === MCP env
# ================================

DB_HOST=localhost
DB_NAME=Airport
DB_USER=postgres
DB_PASSWORD=1111
DB_PORT=5432

MCP_PORT=12345